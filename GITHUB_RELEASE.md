# Как создать релиз через GitHub Actions

## 🚀 Быстрый старт

### 1. Подготовка (один раз)

Добавьте приватный ключ в GitHub Secrets:

1. Прочитайте ключ:
```bash
cat ~/.tauri/ts6overlay.key
```

2. Перейдите в ваш репозиторий на GitHub
3. Settings → Secrets and variables → Actions
4. Нажмите "New repository secret"
5. Name: `TAURI_PRIVATE_KEY`
6. Value: вставьте содержимое ключа
7. Нажмите "Add secret"

### 2. Создание релиза (каждый раз)

```bash
# 1. Убедитесь, что все изменения закоммичены
git add .
git commit -m "Release v1.1.0"

# 2. Создайте и запушьте тег
git tag v1.1.0
git push origin main
git push origin v1.1.0
```

**Всё!** GitHub Actions автоматически:
- ✅ Соберет приложение для macOS (Universal)
- ✅ Соберет приложение для Windows (x64)
- ✅ Подпишет все файлы
- ✅ Создаст GitHub Release
- ✅ Загрузит установочные файлы
- ✅ Создаст latest.json для автообновлений

### 3. Проверка

1. Перейдите в раздел "Actions" вашего репозитория
2. Дождитесь завершения workflow (~10-15 минут)
3. Перейдите в "Releases"
4. Убедитесь, что релиз создан с файлами:
   - `TeamSpeak.OverLive_1.1.0_universal.dmg` (macOS)
   - `TeamSpeak.OverLive_1.1.0_x64_en-US.msi` (Windows)
   - `latest.json` (для автообновлений)
   - Файлы подписей (.sig)

## 📋 Чеклист перед релизом

- [ ] Версия обновлена в `package.json`
- [ ] Версия обновлена в `src-tauri/tauri.conf.json`
- [ ] `CHANGELOG.md` обновлен
- [ ] `RELEASE_NOTES_v1.1.0.md` создан
- [ ] Приложение протестировано локально
- [ ] `TAURI_PRIVATE_KEY` добавлен в GitHub Secrets
- [ ] Все изменения закоммичены

## 🔧 Что делает GitHub Actions

### Workflow запускается при:
```yaml
on:
  push:
    tags:
      - 'v*'  # Любой тег начинающийся с 'v'
```

### Процесс сборки:

1. **Checkout** - клонирует репозиторий
2. **Setup Node** - устанавливает Node.js 20
3. **Install Rust** - устанавливает Rust toolchain
4. **Install dependencies** - `npm install`
5. **Build and release** - собирает и публикует:
   - Использует `tauri-apps/tauri-action@v0`
   - Подписывает файлы с помощью `TAURI_PRIVATE_KEY`
   - Создает GitHub Release
   - Загружает все файлы
   - Генерирует `latest.json` автоматически

### Параллельная сборка:

Workflow запускается на двух платформах одновременно:
- `macos-latest` → Universal Binary (Intel + Apple Silicon)
- `windows-latest` → x64 MSI

## 🐛 Решение проблем

### Workflow не запускается:
- Проверьте, что тег начинается с `v` (например, `v1.1.0`)
- Убедитесь, что тег запушен: `git push origin v1.1.0`

### Ошибка подписи:
- Проверьте, что `TAURI_PRIVATE_KEY` добавлен в Secrets
- Убедитесь, что ключ скопирован полностью (включая первую и последнюю строки)

### Релиз не создается:
- Проверьте права доступа в Settings → Actions → General
- Убедитесь, что включено "Read and write permissions"

### Файлы не загружаются:
- Проверьте логи в разделе Actions
- Убедитесь, что сборка завершилась успешно

## 📝 Пример полного процесса

```bash
# 1. Обновите версию
# Отредактируйте package.json и tauri.conf.json

# 2. Обновите документацию
# Отредактируйте CHANGELOG.md и создайте RELEASE_NOTES_v1.1.0.md

# 3. Закоммитьте изменения
git add .
git commit -m "Release v1.1.0: Mute button, smart sorting, stability improvements"

# 4. Создайте тег
git tag v1.1.0

# 5. Запушьте всё
git push origin main
git push origin v1.1.0

# 6. Дождитесь завершения GitHub Actions
# Перейдите в Actions → Release → Посмотрите прогресс

# 7. Проверьте релиз
# Перейдите в Releases → Убедитесь, что все файлы загружены

# 8. Протестируйте автообновление
# Установите старую версию и проверьте, что обновление работает
```

## 🎉 Готово!

Теперь при каждом push тега `v*` GitHub Actions автоматически создаст релиз для обеих платформ с автообновлениями!

---

**Важно:** Первый релиз может занять больше времени из-за кэширования зависимостей. Последующие релизы будут быстрее.
