# Настройка автообновлений - Пошаговая инструкция

## ✅ Что уже сделано:

1. **Ключи сгенерированы:**
   - Приватный ключ: `~/.tauri/ts6overlay.key` (ХРАНИТЕ В СЕКРЕТЕ!)
   - Публичный ключ: добавлен в `tauri.conf.json`

2. **Updater включен:**
   - `tauri.conf.json` → `"active": true`
   - Добавлен публичный ключ для проверки подписей

3. **Скрипты созданы:**
   - `generate-updater-json.sh` - генерация updater.json после сборки

## 📝 Что нужно сделать:

### 1. Настройте GitHub репозиторий

Замените `YOUR_USERNAME` на ваш GitHub username в файлах:

**tauri.conf.json:**
```json
"endpoints": [
  "https://github.com/YOUR_USERNAME/TS6Overlay/releases/latest/download/latest.json"
]
```

**generate-updater-json.sh:**
```bash
GITHUB_USER="YOUR_USERNAME"
GITHUB_REPO="TS6Overlay"
```

### 2. Добавьте приватный ключ в GitHub Secrets

1. Прочитайте приватный ключ:
```bash
cat ~/.tauri/ts6overlay.key
```

2. Перейдите в Settings → Secrets and variables → Actions вашего GitHub репозитория

3. Добавьте секрет:
   - Name: `TAURI_PRIVATE_KEY`
   - Value: содержимое файла `~/.tauri/ts6overlay.key`

### 3. Обновите GitHub Actions workflow

Добавьте в `.github/workflows/release.yml`:

```yaml
env:
  TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
  TAURI_KEY_PASSWORD: ""  # Пустой пароль
```

### 4. Процесс релиза

#### Локальная сборка:

```bash
# 1. Установите переменную окружения
export TAURI_PRIVATE_KEY=$(cat ~/.tauri/ts6overlay.key)
export TAURI_KEY_PASSWORD=""

# 2. Соберите приложение
npm run build:mac  # или build:win

# 3. Сгенерируйте updater.json
./generate-updater-json.sh

# 4. Создайте GitHub Release и загрузите:
# - Установочные файлы (.dmg, .msi)
# - Архивы для updater (.tar.gz, .zip)
# - Файлы подписей (.sig)
# - updater.json (переименуйте в latest.json)
```

#### Через GitHub Actions:

```bash
# Просто создайте тег и запушьте
git tag v1.1.0
git push origin v1.1.0

# GitHub Actions автоматически:
# - Соберет приложение
# - Подпишет файлы
# - Создаст релиз
# - Загрузит все файлы
```

## 🔍 Проверка работы автообновлений

### Тестирование:

1. Установите версию 1.0.1
2. Опубликуйте релиз 1.1.0 с latest.json
3. Запустите приложение версии 1.0.1
4. Через ~1 минуту должно появиться окно с предложением обновиться
5. Нажмите "Update" и дождитесь установки

### Отладка:

Если обновление не работает, проверьте:

1. **Endpoint доступен:**
```bash
curl https://github.com/YOUR_USERNAME/TS6Overlay/releases/latest/download/latest.json
```

2. **Подпись корректна:**
   - Файлы .sig должны быть в релизе
   - Публичный ключ в tauri.conf.json совпадает с приватным

3. **Версия в latest.json больше текущей:**
   - latest.json → "version": "1.1.0"
   - Текущая версия < 1.1.0

## 📦 Структура релиза

Каждый релиз должен содержать:

### macOS:
- `TeamSpeak OverLive_1.1.0_universal.dmg` - установщик
- `TeamSpeak OverLive_1.1.0_universal.dmg.tar.gz` - для updater
- `TeamSpeak OverLive_1.1.0_universal.dmg.tar.gz.sig` - подпись

### Windows:
- `TeamSpeak OverLive_1.1.0_x64_en-US.msi` - установщик
- `TeamSpeak OverLive_1.1.0_x64_en-US.msi.zip` - для updater
- `TeamSpeak OverLive_1.1.0_x64_en-US.msi.zip.sig` - подпись

### Общее:
- `latest.json` - информация об обновлении (переименованный updater.json)

## ⚠️ Важно!

1. **НИКОГДА не коммитьте приватный ключ** (`~/.tauri/ts6overlay.key`)
2. **Храните резервную копию** приватного ключа в безопасном месте
3. **Без приватного ключа** вы не сможете подписывать обновления
4. **Тестируйте обновления** перед публикацией релиза

## 🔗 Полезные ссылки

- [Tauri Updater Documentation](https://tauri.app/v1/guides/distribution/updater)
- [GitHub Releases](https://docs.github.com/en/repositories/releasing-projects-on-github)

---

**Статус:** ✅ Автообновления настроены и готовы к использованию!
